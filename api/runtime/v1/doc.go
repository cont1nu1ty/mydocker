// Package v1 defines the stable JSON schema for mydocker's first versioned
// node-local lifecycle API. It intentionally contains transport DTOs rather
// than aliases to internal runtime or persistence models.
package v1
